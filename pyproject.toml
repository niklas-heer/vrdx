[project]
name = "vrdx"
version = "0.1.0"
description = "CLI tool for capturing and managing decision records in your repo."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nuitka>=2.7.16",
]

## Implementation

- [x] Align stable Rust pins and retain strict gates, nextest and editor tools.
- [x] Remove Python checkout remnants and replace the installed Python vrdx with the Rust binary.
- [x] Add Dagger/Dang Linux pipeline and thin workflow while preserving native macOS checks.
- [x] Verify module discovery, successful pipeline and failure propagation from an isolated malformed source copy.
- [x] Run native checks, actionlint and hub tooling audit; document setup and capture the decision.
